function setupAuth(mode){const form=document.getElementById('form');const error=document.getElementById('error');form.addEventListener('submit',async e=>{e.preventDefault();error.textContent='';if(mode==='register'&&password.value!==confirm.value){error.textContent='Пароли не совпадают';return}try{const r=await fetch('/api/auth/'+mode,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({nickname:nickname.value,password:password.value})});const d=await r.json();if(!r.ok){error.textContent=d.error||'Произошла ошибка';return}localStorage.setItem('epmToken',d.token);localStorage.setItem('user',JSON.stringify(d.user));location.href='profile.html'}catch{error.textContent='Сервер недоступен'}})}
